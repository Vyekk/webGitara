import styles from './views/TabulatureEditorView/TabulatureEditorView.module.scss';

const TabulatureEditorView = () => {
    return (
        <div className={styles.tabulatureEditorView}>
            <h1>Tabulature Editor</h1>
            <p>This is the tabulature editor view.</p>
        </div>
    );
};

export default TabulatureEditorView;
