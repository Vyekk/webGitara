import React from "react";
import styles from "components/Footer/Footer.module.scss";

const Footer = () => (
    <footer className={styles.wrapper}>
        <p>Copyright &copy; by Konrad Koluch</p>
    </footer>
);

export default Footer;